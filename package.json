{
  "name": "jsmusicdbnext-prebuilt",
  "description": "prebuilt stable versions of jsmusicdbnext",
  "version": "2.29.3",
  "dependencies": {},
  "scripts": {
    "postversion": "npm publish && git push && git push --tags"
  }
}
